@model IEnumerable<DiscussionForum.Models.Video>

@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model.Select(x=>x.VideoCategory).Distinct())
{

     <h3>   @Html.ActionLink(item, "VideoPlayList", new { videoCat = item })</h3>

}


